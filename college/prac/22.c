#include <stdio.h>

int main(){
    char str[] = "h38dgi 1 !0__";
    int sum=0;

    for(int i=0; i< str[i] != '\0'; i++){
        sum = i;
    }
    printf("total %d no. of characters.", sum + 1);

    return 0;
}